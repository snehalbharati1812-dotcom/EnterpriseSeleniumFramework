<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE suite SYSTEM "https://testng.org/testng-1.0.dtd">
<suite name="Smoke Test Suite">
    <listeners>
        <listener class-name="com.company.framework.listeners.TestListener"/>
    </listeners>

    <test name="Smoke Executions">
        <!-- Runs only tests tagged with groups = {"smoke"} -->
        <groups>
            <run>
                <include name="smoke"/>
            </run>
        </groups>

        <!-- Scans the entire package for test classes -->
        <packages>
            <package name="com.company.framework.tests"/>
        </packages>
    </test>
</suite>